events {}

http {
    server {
        listen 80;

        root /usr/share/nginx/html;

        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }

        # Backend Proxy
        location /api/ {
            proxy_pass http://paperless-app:8080/api/;
        }
    }
}
